# Frontend i18n (zh / en) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a runtime Chinese/English language switcher to the `frontend/` SPA (UI chrome strings only; charts, backend logs, and dynamic run text stay English).

**Architecture:** A tiny new `frontend/js/i18n.js` exposes `window.I18N` with `t(key)`, `setLang(lang)`, `getLang()`, `applyI18n(root)` and a flat `{ zh, en }` dictionary. Static HTML strings gain `data-i18n="..."` / `data-i18n-attr="..."` annotations; `app.js` render functions call `I18N.t(...)`. A two-segment `[EN | 中文]` toggle sits in the header; Settings gets a mirrored "Language" radio. Initial language follows `navigator.language`; user choice is persisted in `localStorage` and applied on future loads. Re-render on switch happens via a `langchange` window event that re-triggers the router.

**Tech Stack:** Vanilla JS (no build step), existing `frontend/` SPA, `localStorage`, DOM `CustomEvent`.

**Testing note:** This repo has no frontend test harness. Verification is manual against the Python dev server (`cd frontend && python3 -m http.server 8090`). Each task that ships user-visible change ends with an explicit manual check list and a commit.

**Spec:** `docs/superpowers/specs/2026-04-18-frontend-i18n-design.md`

---

## File Map

**Create:**
- `frontend/js/i18n.js` — dictionary + API (≈ 200 lines including strings)

**Modify:**
- `frontend/index.html` — load `i18n.js` before `app.js`; add `data-i18n` on sidebar / breadcrumb / status bar / project selector; insert `.lang-toggle` in `.header-right`
- `frontend/css/style.css` — `.lang-toggle` and `.lang-btn` styles
- `frontend/js/app.js`:
  - top of IIFE: `const t = (k, v) => (window.I18N ? window.I18N.t(k, v) : k);`
  - `navigate()` — use `t('nav.<view>')` for breadcrumb labels
  - `renderDashboard()`, `renderQuickAction()`, `renderToolInfo()`, `renderComingSoon()`, `renderEmptyState()`, `renderModule()`, `renderQC()`, `renderTrimming()`, `renderStarIndex()`, `renderStarAlign()`, `renderDifferential()`, `renderNetwork()`, `renderCustomPlotPanel()`, `renderSettings()`, `renderLogPanel()` — replace in-scope literal strings with `t(...)`
  - `setupEvents()` — attach `.lang-btn` click handler + `langchange` listener that re-navigates; also sync header / Settings radio
  - `runModule()` — the "Running …" status text uses the module name from `t('nav.<id>')`
  - `init()` — call `I18N.applyI18n(document)` once before first `navigate()`

---

## Task 1 — Create `i18n.js` skeleton + zh/en dictionary (chrome keys)

**Files:**
- Create: `frontend/js/i18n.js`

- [ ] **Step 1: Create `frontend/js/i18n.js` with dictionary and API**

Create the file with this exact content:

```js
/* ============================================================
   RustBrain — Frontend i18n
   Tiny dictionary + t()/setLang()/applyI18n() — no deps.
   ============================================================ */
(function () {
  'use strict';

  const STORAGE_KEY = 'rustbrain.lang';
  const SUPPORTED = ['zh', 'en'];

  const DICT = {
    en: {
      brand: { name: 'RustBrain', subtitle: 'Transcriptomics Platform' },
      nav: {
        overview: 'Overview',
        pipeline: 'Analysis Pipeline',
        alignment_quant: 'Alignment & Quantification',
        system: 'System',
        dashboard: 'Dashboard',
        qc: 'QC Analysis',
        trimming: 'Adapter Trimming',
        alignment: 'Read Alignment',
        quantification: 'Quantification',
        differential: 'Differential Expr.',
        network: 'Network Analysis',
        enrichment: 'Enrichment',
        star_index: 'STAR Index',
        star_align: 'STAR Alignment',
        settings: 'Settings',
      },
      badge: {
        ready: 'Ready', soon: 'Soon', available: 'Available',
        coming_soon: 'Coming Soon', running: 'Running', done: 'Done',
        in_development: 'In Development',
      },
      status: {
        ready: 'Ready',
        no_jobs: 'No active jobs',
        one_job: '1 active job',
        backend_connected: 'Backend Connected',
        running_prefix: 'Running',
      },
      project: {
        no_project: 'No Project',
        none_open: 'No project open',
        new: 'New Project',
        open: 'Open Project',
        prompt_name: 'Enter project name:',
        open_badge: 'Open',
        closed_badge: 'Closed',
        section_title: 'Project',
      },
      common: {
        run: 'Run', cancel: 'Cancel', save: 'Save', close: 'Close', reset: 'Reset',
        browse: 'Browse…', select_files: 'Select Files', select_dir: 'Select Directory',
        loading: 'Loading…',
        empty_no_files: 'No files selected',
        advanced_options: 'Advanced Options',
        log_panel: 'Log',
        export_tsv: 'Export TSV',
        module_not_found: 'Module not found',
      },
      dashboard: {
        title_accent: 'Transcriptomics',
        title_rest: 'Pipeline',
        subtitle: 'End-to-end RNA-seq analysis powered by Rust. From raw reads to biological insights.',
        pipeline_section: 'Analysis Pipeline',
        modules_badge: '{n} modules',
        stat_modules_ready: 'Modules Ready',
        stat_rust_tools: 'Rust Tools',
        stat_active_jobs: 'Active Jobs',
        stat_speed_gain: 'Speed Gain',
        quick_start: 'Quick Start',
        tool_suite: 'Rust Tool Suite',
        quick: {
          qc_title: 'Run QC', qc_desc: 'FastQC quality analysis',
          trim_title: 'Trim Reads', trim_desc: 'Adapter removal',
          deseq_title: 'DESeq2 Analysis', deseq_desc: 'Differential expression',
          wgcna_title: 'WGCNA', wgcna_desc: 'Co-expression network',
        },
        tool_desc: {
          fastqc: '2.1-4.7x faster than Java FastQC',
          cutadapt: 'Byte-identical to Python cutadapt',
          deseq2: '28x faster, 99.6% accuracy vs R',
          wgcna: 'Bit-exact co-expression analysis',
        },
      },
      module: {
        soon_body: 'This module is under development. The {tool} backend integration is being prepared.',
        powered_by: 'Powered by',
      },
      qc: {
        input_files: 'Input Files',
        files_count: '{n} files',
        drop_text: 'Drop FASTQ / BAM files here',
        drop_hint: 'Supports .fastq, .fq, .fastq.gz, .bam, .sam',
        parameters: 'Parameters',
        threads: 'Threads',
        format: 'Format',
        format_auto: 'Auto-detect',
        output_dir: 'Output Directory',
        output_dir_ph: '/path/to/output',
        casava: 'CASAVA mode',
        nogroup: 'Disable base grouping',
        kmer: 'K-mer Size',
        run_qc: 'Run QC',
        results: 'Results',
        tab_quality: 'Quality Scores',
        tab_summary: 'Summary',
        tab_log: 'Log',
        metric_overall: 'Overall',
        metric_mean_quality: 'Mean Quality',
        metric_total_reads: 'Total Reads',
        metric_read_length: 'Read Length',
        col_module: 'Module',
        col_status: 'Status',
      },
      trimming: {
        input_files: 'Input Files',
        drop_text: 'Drop FASTQ / FASTA files here',
        drop_hint: 'Supports .fastq, .fq, .fasta (plain or gzipped)',
        adapter_settings: 'Adapter Settings',
        adapter_preset: 'Adapter Preset',
        preset_illumina: 'Illumina Universal (AGATCGGAAGAGC)',
        preset_nextera: 'Nextera Transposase',
        preset_smallrna: 'Illumina Small RNA',
        preset_bgi: 'BGIseq',
        preset_custom: 'Custom Sequence',
        adapter_3: "3' Adapter (-a)",
        adapter_3_hint: "Sequence to trim from 3' end",
        quality_cutoff: 'Quality Cutoff (-q)',
        min_length: 'Min Length (-m)',
        max_n: 'Max N Bases',
        max_n_hint: '-1 = no limit',
        threads: 'Threads',
        paired_options: 'Paired-End Options',
        paired_mode: 'Paired-end mode',
        adapter_r2: 'R2 Adapter (-A)',
        trim_galore: 'Trim Galore Mode',
        enable_galore: 'Enable Trim Galore wrapper',
        post_fastqc: 'Run FastQC after trimming',
        rrbs: 'RRBS mode',
        run_trim: 'Run Trimming',
        results: 'Trimming Results',
        tab_stats: 'Statistics',
        tab_chart: 'Length Distribution',
        metric_reads_processed: 'Reads Processed',
        metric_reads_passing: 'Reads Passing',
        metric_adapter_found: 'Adapter Found',
        metric_mean_length: 'Mean Length',
        col_metric: 'Metric',
        col_value: 'Value',
      },
      star_index: {
        title: 'STAR Genome Index',
        desc: 'Build a STAR index from a reference genome FASTA and GTF annotation. Required before any alignment run.',
        genome_fasta: 'Genome FASTA',
        gtf: 'GTF annotation',
        threads: 'Threads',
        sjdb: 'sjdbOverhang',
        sa_nbases: 'genomeSAindexNbases',
        advanced: 'Advanced',
        extra_args: 'Extra args (one per line)',
        submit: 'Build Index',
      },
      star_align: {
        title: 'STAR Alignment & Quantification',
        desc: 'Map FASTQ reads to a pre-built STAR index and produce per-sample BAM, gene counts, and a merged counts matrix.',
        genome_dir: 'Genome index directory',
        reads_1: 'R1 FASTQ files (one per sample)',
        reads_2: 'R2 FASTQ files (optional, paired-end)',
        sample_names: 'Sample names (optional, one per line; defaults from R1 filename)',
        threads: 'Threads',
        strand: 'Strand',
        strand_unstranded: 'unstranded',
        strand_forward: 'forward',
        strand_reverse: 'reverse',
        advanced: 'Advanced',
        extra_args: 'Extra args (one per line)',
        submit: 'Run Alignment',
        mapping_rate: 'Mapping rate',
        matrix_preview: 'Counts matrix preview (first 50 × 10)',
        no_matrix: 'No counts matrix produced',
        use_in_deseq: 'Use this matrix in DESeq2',
      },
      differential: {
        input_data: 'Input Data',
        counts_matrix: 'Count Matrix (TSV)',
        counts_matrix_ph: '/path/to/counts_matrix.tsv',
        drop_counts: 'Drop counts matrix file',
        drop_counts_hint: 'Genes in rows, samples in columns (TSV)',
        sample_info: 'Sample Information (TSV)',
        drop_coldata: 'Drop coldata / sample info',
        drop_coldata_hint: 'Sample names, conditions, covariates',
        parameters: 'DESeq2 Parameters',
        design_var: 'Design Variable',
        design_var_ph: 'e.g. condition, treatment',
        design_var_hint: 'Column in sample info for comparison',
        ref_level: 'Reference Level',
        ref_level_ph: 'e.g. control, untreated',
        ref_level_hint: 'Baseline for fold-change calculation',
        padj: 'padj Cutoff',
        lfc: '|log2FC| Cutoff',
        output_file: 'Output File',
        run_deseq: 'Run DESeq2',
        results: 'Analysis Results',
        tab_volcano: 'Volcano Plot',
        tab_ma: 'MA Plot',
        tab_table: 'Results Table',
        tab_custom: 'Custom Plot',
        metric_total: 'Total Genes',
        metric_up: 'Up-regulated',
        metric_down: 'Down-regulated',
        metric_sig: 'Significant',
        col_gene: 'Gene',
        col_lfc: 'log2FC',
        col_pvalue: 'p-value',
        col_padj: 'padj',
      },
      network: {
        input_data: 'Input Data',
        expr_matrix: 'Expression Matrix (CSV)',
        drop_expr: 'Drop expression matrix',
        drop_expr_hint: 'Samples in rows, genes in columns',
        trait_data: 'Trait Data (optional)',
        drop_trait: 'Drop trait data',
        drop_trait_hint: 'For module-trait association',
        parameters: 'WGCNA Parameters',
        corr_method: 'Correlation Method',
        corr_pearson: 'Pearson',
        corr_biweight: 'Biweight Midcorrelation',
        net_type: 'Network Type',
        net_signed: 'Signed',
        net_unsigned: 'Unsigned',
        net_signed_hybrid: 'Signed Hybrid',
        soft_thresh: 'Soft Threshold',
        soft_thresh_hint: 'Use threshold picker',
        min_module: 'Min Module Size',
        merge_cut: 'Merge Cut Height',
        tom_type: 'TOM Type',
        pam: 'PAM refinement',
        deep_split: 'Deep Split',
        deep_default: '2 (default)',
        cytoscape: 'Export Cytoscape network',
        pick_threshold: 'Pick Threshold',
        run_wgcna: 'Run WGCNA',
        results: 'Network Results',
        tab_modules: 'Modules',
        tab_trait: 'Trait Heatmap',
        tab_custom: 'Custom Plot',
        metric_genes: 'Genes',
        metric_modules: 'Modules',
        metric_fit: 'Scale-Free Fit',
      },
      custom_plot: {
        title: 'Custom Plot Builder',
      },
      settings: {
        title: 'Settings',
        binary_section: 'Binary Paths',
        binary_title: 'Settings — Binary Paths',
        binary_intro_html: 'Resolution order: <strong>configured</strong> (your override) → <strong>bundled</strong> (shipped with the installed app) → <strong>PATH</strong>. Your override always wins.',
        col_tool: 'Tool',
        col_configured: 'Configured',
        col_bundled: 'Bundled',
        col_path: 'Detected on PATH',
        col_status: 'Status',
        col_actions: 'Actions',
        not_set: '(not set)',
        not_bundled: '(not bundled)',
        not_on_path: '(not on PATH)',
        ok: 'OK',
        missing: 'Missing',
        clear: 'Clear',
        language_section: 'Language',
        language_section_full: 'Language / 语言',
        language_en: 'English',
        language_zh: '中文',
      },
    },
    zh: {
      brand: { name: 'RustBrain', subtitle: '转录组分析平台' },
      nav: {
        overview: '总览',
        pipeline: '分析流程',
        alignment_quant: '比对与定量',
        system: '系统',
        dashboard: '仪表盘',
        qc: '质量控制',
        trimming: '接头修剪',
        alignment: '序列比对',
        quantification: '定量',
        differential: '差异表达',
        network: '网络分析',
        enrichment: '功能富集',
        star_index: 'STAR 索引',
        star_align: 'STAR 比对',
        settings: '设置',
      },
      badge: {
        ready: '就绪', soon: '即将推出', available: '可用',
        coming_soon: '即将推出', running: '运行中', done: '完成',
        in_development: '开发中',
      },
      status: {
        ready: '就绪',
        no_jobs: '无活动任务',
        one_job: '1 个活动任务',
        backend_connected: '后端已连接',
        running_prefix: '运行',
      },
      project: {
        no_project: '未选择项目',
        none_open: '未打开项目',
        new: '新建项目',
        open: '打开项目',
        prompt_name: '请输入项目名称：',
        open_badge: '已打开',
        closed_badge: '未打开',
        section_title: '项目',
      },
      common: {
        run: '运行', cancel: '取消', save: '保存', close: '关闭', reset: '重置',
        browse: '浏览…', select_files: '选择文件', select_dir: '选择目录',
        loading: '加载中…',
        empty_no_files: '尚未选择文件',
        advanced_options: '高级选项',
        log_panel: '日志',
        export_tsv: '导出 TSV',
        module_not_found: '未找到该模块',
      },
      dashboard: {
        title_accent: '转录组',
        title_rest: '分析流程',
        subtitle: '由 Rust 驱动的端到端 RNA-seq 分析，从原始读段到生物学洞见。',
        pipeline_section: '分析流程',
        modules_badge: '{n} 个模块',
        stat_modules_ready: '可用模块',
        stat_rust_tools: 'Rust 工具',
        stat_active_jobs: '活动任务',
        stat_speed_gain: '加速倍数',
        quick_start: '快速开始',
        tool_suite: 'Rust 工具套件',
        quick: {
          qc_title: '运行 QC', qc_desc: 'FastQC 质量分析',
          trim_title: '接头修剪', trim_desc: '去除接头',
          deseq_title: 'DESeq2 分析', deseq_desc: '差异表达分析',
          wgcna_title: 'WGCNA', wgcna_desc: '共表达网络',
        },
        tool_desc: {
          fastqc: '较 Java FastQC 快 2.1–4.7 倍',
          cutadapt: '与 Python cutadapt 字节一致',
          deseq2: '较 R 快 28 倍，准确率 99.6%',
          wgcna: '位精确共表达分析',
        },
      },
      module: {
        soon_body: '此模块正在开发中，{tool} 后端整合正在准备中。',
        powered_by: '基于',
      },
      qc: {
        input_files: '输入文件',
        files_count: '{n} 个文件',
        drop_text: '拖入 FASTQ / BAM 文件',
        drop_hint: '支持 .fastq、.fq、.fastq.gz、.bam、.sam',
        parameters: '参数',
        threads: '线程数',
        format: '格式',
        format_auto: '自动检测',
        output_dir: '输出目录',
        output_dir_ph: '/path/to/output',
        casava: 'CASAVA 模式',
        nogroup: '禁用碱基分组',
        kmer: 'K-mer 大小',
        run_qc: '运行 QC',
        results: '结果',
        tab_quality: '质量分数',
        tab_summary: '概览',
        tab_log: '日志',
        metric_overall: '总体',
        metric_mean_quality: '平均质量',
        metric_total_reads: '总读段数',
        metric_read_length: '读段长度',
        col_module: '模块',
        col_status: '状态',
      },
      trimming: {
        input_files: '输入文件',
        drop_text: '拖入 FASTQ / FASTA 文件',
        drop_hint: '支持 .fastq、.fq、.fasta（原文或 gzip）',
        adapter_settings: '接头设置',
        adapter_preset: '接头预设',
        preset_illumina: 'Illumina 通用 (AGATCGGAAGAGC)',
        preset_nextera: 'Nextera 转座酶',
        preset_smallrna: 'Illumina 小 RNA',
        preset_bgi: 'BGIseq',
        preset_custom: '自定义序列',
        adapter_3: "3' 端接头 (-a)",
        adapter_3_hint: "从 3' 端修剪的序列",
        quality_cutoff: '质量阈值 (-q)',
        min_length: '最短长度 (-m)',
        max_n: '最多 N 碱基',
        max_n_hint: '-1 = 不限制',
        threads: '线程数',
        paired_options: '双端选项',
        paired_mode: '双端模式',
        adapter_r2: 'R2 接头 (-A)',
        trim_galore: 'Trim Galore 模式',
        enable_galore: '启用 Trim Galore 包装',
        post_fastqc: '修剪后运行 FastQC',
        rrbs: 'RRBS 模式',
        run_trim: '运行修剪',
        results: '修剪结果',
        tab_stats: '统计',
        tab_chart: '长度分布',
        metric_reads_processed: '处理读段',
        metric_reads_passing: '通过读段',
        metric_adapter_found: '发现接头',
        metric_mean_length: '平均长度',
        col_metric: '指标',
        col_value: '数值',
      },
      star_index: {
        title: 'STAR 基因组索引',
        desc: '从参考基因组 FASTA 和 GTF 注释构建 STAR 索引，任何比对运行之前必须完成。',
        genome_fasta: '基因组 FASTA',
        gtf: 'GTF 注释',
        threads: '线程数',
        sjdb: 'sjdbOverhang',
        sa_nbases: 'genomeSAindexNbases',
        advanced: '高级',
        extra_args: '额外参数（每行一个）',
        submit: '构建索引',
      },
      star_align: {
        title: 'STAR 比对与定量',
        desc: '将 FASTQ 读段比对到预建的 STAR 索引，产出每个样本的 BAM、基因计数，以及合并后的计数矩阵。',
        genome_dir: '基因组索引目录',
        reads_1: 'R1 FASTQ 文件（每个样本一个）',
        reads_2: 'R2 FASTQ 文件（可选，双端）',
        sample_names: '样本名（可选，每行一个；默认取自 R1 文件名）',
        threads: '线程数',
        strand: '链特异性',
        strand_unstranded: '非链特异',
        strand_forward: '正向',
        strand_reverse: '反向',
        advanced: '高级',
        extra_args: '额外参数（每行一个）',
        submit: '运行比对',
        mapping_rate: '比对率',
        matrix_preview: '计数矩阵预览（前 50 × 10）',
        no_matrix: '未生成计数矩阵',
        use_in_deseq: '在 DESeq2 中使用此矩阵',
      },
      differential: {
        input_data: '输入数据',
        counts_matrix: '计数矩阵 (TSV)',
        counts_matrix_ph: '/path/to/counts_matrix.tsv',
        drop_counts: '拖入计数矩阵文件',
        drop_counts_hint: '基因为行，样本为列 (TSV)',
        sample_info: '样本信息 (TSV)',
        drop_coldata: '拖入 coldata / 样本信息',
        drop_coldata_hint: '样本名、条件、协变量',
        parameters: 'DESeq2 参数',
        design_var: '设计变量',
        design_var_ph: '例如 condition、treatment',
        design_var_hint: '样本信息中用于比较的列',
        ref_level: '参考水平',
        ref_level_ph: '例如 control、untreated',
        ref_level_hint: '倍数变化计算的基线',
        padj: 'padj 阈值',
        lfc: '|log2FC| 阈值',
        output_file: '输出文件',
        run_deseq: '运行 DESeq2',
        results: '分析结果',
        tab_volcano: '火山图',
        tab_ma: 'MA 图',
        tab_table: '结果表',
        tab_custom: '自定义图',
        metric_total: '总基因数',
        metric_up: '上调',
        metric_down: '下调',
        metric_sig: '显著',
        col_gene: '基因',
        col_lfc: 'log2FC',
        col_pvalue: 'p-value',
        col_padj: 'padj',
      },
      network: {
        input_data: '输入数据',
        expr_matrix: '表达矩阵 (CSV)',
        drop_expr: '拖入表达矩阵',
        drop_expr_hint: '样本为行，基因为列',
        trait_data: '性状数据（可选）',
        drop_trait: '拖入性状数据',
        drop_trait_hint: '用于模块-性状关联',
        parameters: 'WGCNA 参数',
        corr_method: '相关方法',
        corr_pearson: 'Pearson',
        corr_biweight: 'Biweight 中位相关',
        net_type: '网络类型',
        net_signed: '有符号',
        net_unsigned: '无符号',
        net_signed_hybrid: '有符号混合',
        soft_thresh: '软阈值',
        soft_thresh_hint: '使用阈值选择器',
        min_module: '最小模块大小',
        merge_cut: '合并切割高度',
        tom_type: 'TOM 类型',
        pam: 'PAM 精修',
        deep_split: 'Deep Split',
        deep_default: '2（默认）',
        cytoscape: '导出 Cytoscape 网络',
        pick_threshold: '选择阈值',
        run_wgcna: '运行 WGCNA',
        results: '网络结果',
        tab_modules: '模块',
        tab_trait: '性状热图',
        tab_custom: '自定义图',
        metric_genes: '基因',
        metric_modules: '模块',
        metric_fit: '无标度拟合',
      },
      custom_plot: {
        title: '自定义绘图',
      },
      settings: {
        title: '设置',
        binary_section: '二进制路径',
        binary_title: '设置 — 二进制路径',
        binary_intro_html: '解析顺序：<strong>已配置</strong>（你的覆盖）→ <strong>随应用打包</strong> → <strong>PATH</strong>。你的覆盖始终优先。',
        col_tool: '工具',
        col_configured: '已配置',
        col_bundled: '打包',
        col_path: 'PATH 中检测到',
        col_status: '状态',
        col_actions: '操作',
        not_set: '（未设置）',
        not_bundled: '（未打包）',
        not_on_path: '（不在 PATH 中）',
        ok: '正常',
        missing: '缺失',
        clear: '清除',
        language_section: '语言',
        language_section_full: '语言 / Language',
        language_en: 'English',
        language_zh: '中文',
      },
    },
  };

  function resolveInitial() {
    try {
      const stored = localStorage.getItem(STORAGE_KEY);
      if (SUPPORTED.includes(stored)) return stored;
    } catch (_) { /* localStorage disabled */ }
    const nav = (navigator.language || 'en').toLowerCase();
    return nav.startsWith('zh') ? 'zh' : 'en';
  }

  let currentLang = resolveInitial();
  document.documentElement.setAttribute('lang', currentLang === 'zh' ? 'zh-CN' : 'en');

  function lookup(dict, key) {
    const parts = key.split('.');
    let node = dict;
    for (const p of parts) {
      if (node == null || typeof node !== 'object') return undefined;
      node = node[p];
    }
    return typeof node === 'string' ? node : undefined;
  }

  function interpolate(str, vars) {
    if (!vars) return str;
    return str.replace(/\{(\w+)\}/g, (_, k) => (vars[k] != null ? String(vars[k]) : '{' + k + '}'));
  }

  function t(key, vars) {
    const s = lookup(DICT[currentLang], key)
           ?? lookup(DICT.en, key)
           ?? key;
    return interpolate(s, vars);
  }

  function applyI18n(root) {
    if (!root) return;
    root.querySelectorAll('[data-i18n]').forEach(el => {
      el.textContent = t(el.dataset.i18n);
    });
    root.querySelectorAll('[data-i18n-attr]').forEach(el => {
      el.dataset.i18nAttr.split(',').forEach(pair => {
        const [attr, key] = pair.split(':').map(s => s && s.trim());
        if (attr && key) el.setAttribute(attr, t(key));
      });
    });
  }

  function setLang(lang) {
    if (!SUPPORTED.includes(lang) || lang === currentLang) return;
    currentLang = lang;
    try { localStorage.setItem(STORAGE_KEY, lang); } catch (_) {}
    document.documentElement.setAttribute('lang', lang === 'zh' ? 'zh-CN' : 'en');
    applyI18n(document);
    window.dispatchEvent(new CustomEvent('langchange', { detail: { lang } }));
  }

  window.I18N = { t, setLang, getLang: () => currentLang, applyI18n };

  // Dev-only asymmetry check — logs any keys missing from the other locale.
  (function checkKeys() {
    const walk = (obj, prefix = '') => {
      const out = [];
      for (const [k, v] of Object.entries(obj)) {
        const path = prefix ? `${prefix}.${k}` : k;
        if (v && typeof v === 'object' && !Array.isArray(v)) out.push(...walk(v, path));
        else out.push(path);
      }
      return out;
    };
    const en = new Set(walk(DICT.en));
    const zh = new Set(walk(DICT.zh));
    const onlyEn = [...en].filter(k => !zh.has(k));
    const onlyZh = [...zh].filter(k => !en.has(k));
    if (onlyEn.length) console.warn('[i18n] keys only in en:', onlyEn);
    if (onlyZh.length) console.warn('[i18n] keys only in zh:', onlyZh);
  })();
})();
```

- [ ] **Step 2: Smoke-check the file in a browser**

Run:
```bash
cd frontend && python3 -m http.server 8090
```

Open `http://localhost:8090/` in a browser. **Nothing changes visibly yet** — `index.html` does not load `i18n.js` yet. Open the browser console and paste:
```js
const s=document.createElement('script'); s.src='js/i18n.js'; document.head.appendChild(s);
setTimeout(()=>{ console.log(window.I18N.getLang(), window.I18N.t('nav.dashboard')); }, 200);
```
Expected: prints either `zh 仪表盘` or `en Dashboard` depending on `navigator.language`. No console errors. No "keys only in …" warnings.

- [ ] **Step 3: Commit**

```bash
git add frontend/js/i18n.js
git commit -m "feat(frontend): add i18n.js with zh/en dictionary and runtime switcher API"
```

---

## Task 2 — Load `i18n.js` + annotate static HTML chrome

**Files:**
- Modify: `frontend/index.html`

- [ ] **Step 1: Add the `<script>` tag before `app.js`**

In `frontend/index.html`, find the line:
```html
  <script src="js/app.js"></script>
```
and insert **immediately before it**:
```html
  <script src="js/i18n.js"></script>
```

- [ ] **Step 2: Annotate sidebar, header, status bar with `data-i18n`**

Apply every edit in this step. Each replacement is a single string.

**Edit A — logo subtitle** (around line 77):
Replace:
```html
          <span class="logo-sub">Transcriptomics Platform</span>
```
With:
```html
          <span class="logo-sub" data-i18n="brand.subtitle">Transcriptomics Platform</span>
```

**Edit B — Overview section title + Dashboard item** (around lines 84–88):
Replace:
```html
        <div class="nav-section-title">Overview</div>
        <a class="nav-item active" data-view="dashboard" href="#dashboard">
          <i data-lucide="layout-dashboard"></i>
          <span>Dashboard</span>
        </a>
```
With:
```html
        <div class="nav-section-title" data-i18n="nav.overview">Overview</div>
        <a class="nav-item active" data-view="dashboard" href="#dashboard">
          <i data-lucide="layout-dashboard"></i>
          <span data-i18n="nav.dashboard">Dashboard</span>
        </a>
```

**Edit C — Analysis Pipeline section** (around line 92):
Replace:
```html
        <div class="nav-section-title">Analysis Pipeline</div>
```
With:
```html
        <div class="nav-section-title" data-i18n="nav.pipeline">Analysis Pipeline</div>
```

**Edit D — 7 pipeline nav items** (lines 93–134). For each nav item, wrap the label text and the badge in `<span>` with `data-i18n`. Replace the entire section body so each nav label + badge is annotated:

Replace:
```html
        <a class="nav-item" data-view="qc" data-color="teal" href="#qc">
          <span class="pipeline-dot" style="--dot-color: var(--mod-teal)"></span>
          <i data-lucide="microscope"></i>
          <span>QC Analysis</span>
          <span class="nav-badge ready">Ready</span>
        </a>
        <a class="nav-item" data-view="trimming" data-color="blue" href="#trimming">
          <span class="pipeline-dot" style="--dot-color: var(--mod-blue)"></span>
          <i data-lucide="scissors"></i>
          <span>Adapter Trimming</span>
          <span class="nav-badge ready">Ready</span>
        </a>
        <a class="nav-item" data-view="alignment" data-color="purple" href="#alignment">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="git-merge"></i>
          <span>Read Alignment</span>
          <span class="nav-badge soon">Soon</span>
        </a>
        <a class="nav-item" data-view="quantification" data-color="gold" href="#quantification">
          <span class="pipeline-dot" style="--dot-color: var(--mod-gold)"></span>
          <i data-lucide="bar-chart-3"></i>
          <span>Quantification</span>
          <span class="nav-badge soon">Soon</span>
        </a>
        <a class="nav-item" data-view="differential" data-color="coral" href="#differential">
          <span class="pipeline-dot" style="--dot-color: var(--mod-coral)"></span>
          <i data-lucide="flame"></i>
          <span>Differential Expr.</span>
          <span class="nav-badge ready">Ready</span>
        </a>
        <a class="nav-item" data-view="network" data-color="green" href="#network">
          <span class="pipeline-dot" style="--dot-color: var(--mod-green)"></span>
          <i data-lucide="share-2"></i>
          <span>Network Analysis</span>
          <span class="nav-badge ready">Ready</span>
        </a>
        <a class="nav-item" data-view="enrichment" data-color="slate" href="#enrichment">
          <span class="pipeline-dot" style="--dot-color: var(--mod-slate)"></span>
          <i data-lucide="target"></i>
          <span>Enrichment</span>
          <span class="nav-badge soon">Soon</span>
        </a>
```

With:
```html
        <a class="nav-item" data-view="qc" data-color="teal" href="#qc">
          <span class="pipeline-dot" style="--dot-color: var(--mod-teal)"></span>
          <i data-lucide="microscope"></i>
          <span data-i18n="nav.qc">QC Analysis</span>
          <span class="nav-badge ready" data-i18n="badge.ready">Ready</span>
        </a>
        <a class="nav-item" data-view="trimming" data-color="blue" href="#trimming">
          <span class="pipeline-dot" style="--dot-color: var(--mod-blue)"></span>
          <i data-lucide="scissors"></i>
          <span data-i18n="nav.trimming">Adapter Trimming</span>
          <span class="nav-badge ready" data-i18n="badge.ready">Ready</span>
        </a>
        <a class="nav-item" data-view="alignment" data-color="purple" href="#alignment">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="git-merge"></i>
          <span data-i18n="nav.alignment">Read Alignment</span>
          <span class="nav-badge soon" data-i18n="badge.soon">Soon</span>
        </a>
        <a class="nav-item" data-view="quantification" data-color="gold" href="#quantification">
          <span class="pipeline-dot" style="--dot-color: var(--mod-gold)"></span>
          <i data-lucide="bar-chart-3"></i>
          <span data-i18n="nav.quantification">Quantification</span>
          <span class="nav-badge soon" data-i18n="badge.soon">Soon</span>
        </a>
        <a class="nav-item" data-view="differential" data-color="coral" href="#differential">
          <span class="pipeline-dot" style="--dot-color: var(--mod-coral)"></span>
          <i data-lucide="flame"></i>
          <span data-i18n="nav.differential">Differential Expr.</span>
          <span class="nav-badge ready" data-i18n="badge.ready">Ready</span>
        </a>
        <a class="nav-item" data-view="network" data-color="green" href="#network">
          <span class="pipeline-dot" style="--dot-color: var(--mod-green)"></span>
          <i data-lucide="share-2"></i>
          <span data-i18n="nav.network">Network Analysis</span>
          <span class="nav-badge ready" data-i18n="badge.ready">Ready</span>
        </a>
        <a class="nav-item" data-view="enrichment" data-color="slate" href="#enrichment">
          <span class="pipeline-dot" style="--dot-color: var(--mod-slate)"></span>
          <i data-lucide="target"></i>
          <span data-i18n="nav.enrichment">Enrichment</span>
          <span class="nav-badge soon" data-i18n="badge.soon">Soon</span>
        </a>
```

**Edit E — Alignment & Quantification section** (lines 137–149):
Replace:
```html
        <div class="nav-section-title">Alignment &amp; Quantification</div>
        <a class="nav-item" data-view="star-index" href="#star-index">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="database"></i>
          <span>STAR Index</span>
        </a>
        <a class="nav-item" data-view="star-align" href="#star-align">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="git-merge"></i>
          <span>STAR Alignment</span>
        </a>
```

With:
```html
        <div class="nav-section-title" data-i18n="nav.alignment_quant">Alignment &amp; Quantification</div>
        <a class="nav-item" data-view="star-index" href="#star-index">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="database"></i>
          <span data-i18n="nav.star_index">STAR Index</span>
        </a>
        <a class="nav-item" data-view="star-align" href="#star-align">
          <span class="pipeline-dot" style="--dot-color: var(--mod-purple)"></span>
          <i data-lucide="git-merge"></i>
          <span data-i18n="nav.star_align">STAR Alignment</span>
        </a>
```

**Edit F — System section + Settings item** (lines 151–156):
Replace:
```html
        <div class="nav-section-title">System</div>
        <a class="nav-item" data-view="settings" href="#settings">
          <i data-lucide="settings"></i>
          <span>Settings</span>
        </a>
```

With:
```html
        <div class="nav-section-title" data-i18n="nav.system">System</div>
        <a class="nav-item" data-view="settings" href="#settings">
          <i data-lucide="settings"></i>
          <span data-i18n="nav.settings">Settings</span>
        </a>
```

**Edit G — Sidebar footer "Backend Connected"** (around line 163):
Replace:
```html
        <span>Backend Connected</span>
```
With:
```html
        <span data-i18n="status.backend_connected">Backend Connected</span>
```

**Edit H — Breadcrumb default** (around lines 176–178):
Replace:
```html
          <span class="breadcrumb-home">RustBrain</span>
          <i data-lucide="chevron-right" class="breadcrumb-sep"></i>
          <span class="breadcrumb-current">Dashboard</span>
```
With:
```html
          <span class="breadcrumb-home" data-i18n="brand.name">RustBrain</span>
          <i data-lucide="chevron-right" class="breadcrumb-sep"></i>
          <span class="breadcrumb-current" data-i18n="nav.dashboard">Dashboard</span>
```

**Edit I — Project selector placeholder** (around line 184):
Replace:
```html
          <span id="projectName">No Project</span>
```
With:
```html
          <span id="projectName" data-i18n="project.no_project">No Project</span>
```

**Edit J — Status bar** (lines 196–207). Replace the entire footer:
```html
  <footer class="status-bar">
    <div class="status-left">
      <span class="status-indicator online"></span>
      <span id="statusText">Ready</span>
    </div>
    <div class="status-center">
      <span id="jobStatus">No active jobs</span>
    </div>
    <div class="status-right">
      <span class="status-tag">Rust</span>
      <span class="status-version">v0.1.0</span>
    </div>
  </footer>
```

With:
```html
  <footer class="status-bar">
    <div class="status-left">
      <span class="status-indicator online"></span>
      <span id="statusText" data-i18n="status.ready">Ready</span>
    </div>
    <div class="status-center">
      <span id="jobStatus" data-i18n="status.no_jobs">No active jobs</span>
    </div>
    <div class="status-right">
      <span class="status-tag">Rust</span>
      <span class="status-version">v0.1.0</span>
    </div>
  </footer>
```

(The "Rust" tag and version are identifiers — deliberately not translated.)

- [ ] **Step 3: Manual check — browser reload**

Run dev server (or reuse it from Task 1):
```bash
cd frontend && python3 -m http.server 8090
```
In a browser with `navigator.language` = `zh-CN`, open `http://localhost:8090/` and verify: sidebar, breadcrumb, "Backend Connected", and status bar now render in Chinese on first paint. Note: `app.js` has not yet been updated, so module views still show English strings — that's expected until Task 5.

In a browser with `navigator.language` = `en-US`, verify the same elements render identical English text (nothing changed for English users). Open DevTools console and run `I18N.setLang('zh')` / `I18N.setLang('en')` and visually confirm sidebar labels switch live.

- [ ] **Step 4: Commit**

```bash
git add frontend/index.html
git commit -m "feat(frontend): load i18n.js and annotate static chrome with data-i18n"
```

---

## Task 3 — Add header language toggle + CSS

**Files:**
- Modify: `frontend/index.html`
- Modify: `frontend/css/style.css`

- [ ] **Step 1: Insert toggle into header**

In `frontend/index.html`, find `.header-right` (around line 181):
```html
      <div class="header-right">
        <div class="project-selector">
```

Replace with:
```html
      <div class="header-right">
        <div class="lang-toggle" role="group" aria-label="Language">
          <button type="button" class="lang-btn" data-lang="en">EN</button>
          <button type="button" class="lang-btn" data-lang="zh">中文</button>
        </div>
        <div class="project-selector">
```

- [ ] **Step 2: Add CSS for the toggle**

In `frontend/css/style.css`, find the `.header-right` block (around line 398) and insert the following block **immediately after** it:

```css
.lang-toggle {
  display: inline-flex;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  background: var(--bg-surface);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
}

.lang-btn {
  font-family: inherit;
  font-size: 0.78rem;
  padding: 5px 10px;
  background: transparent;
  border: 0;
  color: var(--text-secondary);
  cursor: pointer;
  transition: background var(--duration-fast) ease, color var(--duration-fast) ease;
}

.lang-btn + .lang-btn {
  border-left: 1px solid var(--border-default);
}

.lang-btn:hover {
  color: var(--text-primary);
}

.lang-btn.active {
  background: var(--bg-elevated, #fff);
  color: var(--text-primary);
  font-weight: 600;
}
```

- [ ] **Step 3: Manual check — toggle renders, clicking it does nothing yet**

Reload the browser. Expected: two buttons `EN | 中文` visible in the header left of the project selector; hovering highlights them; clicking does nothing (handler is wired in Task 4).

- [ ] **Step 4: Commit**

```bash
git add frontend/index.html frontend/css/style.css
git commit -m "feat(frontend): add header language toggle markup and styles"
```

---

## Task 4 — Wire switcher into `app.js` + re-render on langchange

**Files:**
- Modify: `frontend/js/app.js`

- [ ] **Step 1: Add `t()` helper at top of IIFE**

In `frontend/js/app.js`, find the line (around line 7):
```js
(function () {
  'use strict';

  // ── Configuration ──────────────────────────────────────────
```

Replace with:
```js
(function () {
  'use strict';

  // ── i18n helper (falls back to key if i18n.js failed to load) ─
  const t = (k, v) => (window.I18N ? window.I18N.t(k, v) : k);

  // ── Configuration ──────────────────────────────────────────
```

- [ ] **Step 2: Sync the header toggle's `.active` state + handle clicks**

In `frontend/js/app.js`, find `function setupEvents() {` (around line 1361). Inside it, at the **very end of the function body, just before the closing brace** (line 1482, before `}`), insert:

```js
    // Language toggle (header buttons)
    document.querySelectorAll('.lang-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        if (window.I18N) window.I18N.setLang(btn.dataset.lang);
      });
    });

    const syncLangButtons = () => {
      const cur = window.I18N ? window.I18N.getLang() : 'en';
      document.querySelectorAll('.lang-btn').forEach(b => {
        b.classList.toggle('active', b.dataset.lang === cur);
      });
    };
    syncLangButtons();

    // Re-render dynamic content when language changes
    window.addEventListener('langchange', () => {
      syncLangButtons();
      // Settings radio, if present, syncs via its own listener in renderSettings()
      navigate(state.currentView);
    });
```

- [ ] **Step 3: Apply i18n to breadcrumb labels in `navigate()`**

In `frontend/js/app.js`, find `function navigate(view) {` (around line 88). Replace the block:
```js
    const bc = document.getElementById('breadcrumb');
    const label = view === 'dashboard' ? 'Dashboard'
      : view === 'settings' ? 'Settings'
      : view === 'star-index' ? 'STAR Index'
      : view === 'star-align' ? 'STAR Alignment'
      : MODULES.find(m => m.id === view)?.name || view;
    bc.innerHTML = `
      <span class="breadcrumb-home">RustBrain</span>
      <i data-lucide="chevron-right" class="breadcrumb-sep"></i>
      <span class="breadcrumb-current">${label}</span>
    `;
```

With:
```js
    const bc = document.getElementById('breadcrumb');
    const KNOWN_VIEWS = new Set([
      'dashboard', 'settings', 'star-index', 'star-align',
      ...MODULES.map(m => m.id),
    ]);
    const label = KNOWN_VIEWS.has(view)
      ? t('nav.' + view.replace(/-/g, '_'))
      : view;
    bc.innerHTML = `
      <span class="breadcrumb-home">${t('brand.name')}</span>
      <i data-lucide="chevron-right" class="breadcrumb-sep"></i>
      <span class="breadcrumb-current">${label}</span>
    `;
```

- [ ] **Step 4: Apply i18n once on init**

In `frontend/js/app.js`, find `function init() {` (around line 1592). Replace:
```js
  function init() {
    setupEvents();
```

With:
```js
  function init() {
    if (window.I18N) window.I18N.applyI18n(document);
    setupEvents();
```

- [ ] **Step 5: Manual check**

Reload the browser. Click `EN | 中文` in the header:
- The active segment highlights and swaps when clicked.
- The breadcrumb updates between `RustBrain › Dashboard` and `RustBrain › 仪表盘`.
- Sidebar labels switch (already wired in Task 2).
- Refresh the page: last choice persists.
- Module views (QC, Trimming, etc.) still show English strings — Task 5 handles those.

- [ ] **Step 6: Commit**

```bash
git add frontend/js/app.js
git commit -m "feat(frontend): wire language toggle and re-render on langchange"
```

---

## Task 5 — Translate render functions (module views)

This task is big by key count but mechanical: for each render function, replace literal UI strings with `${t('...')}` using the dictionary keys already present in `i18n.js`. Do not change HTML structure, classes, `id`s, or event handlers.

**Files:**
- Modify: `frontend/js/app.js`

- [ ] **Step 1: `renderDashboard()` (line ≈129) and its helpers**

Replace the entire body of `renderDashboard()` with the i18n'd version below. The `pipelineNodes` template changes the status line; projStatus badges change; title/subtitle/cards use `t()`:

```js
  function renderDashboard() {
    const pipelineNodes = MODULES.map((m, i) => {
      const connector = i < MODULES.length - 1
        ? '<div class="pipeline-connector"><div class="pipeline-connector-line"></div></div>'
        : '';
      const nameKey = 'nav.' + m.id.replace(/-/g, '_');
      return `
        <div class="pipeline-stage animate-slide-up" style="animation-delay: ${i * 60}ms">
          <div class="pipeline-node ${m.status}" data-view="${m.id}" style="--node-color: ${COLOR_MAP[m.color]}">
            <div class="pipeline-node-icon"><i data-lucide="${m.icon}"></i></div>
            <div class="pipeline-node-title">${t(nameKey)}</div>
            <div class="pipeline-node-desc">${m.tool}</div>
            <div class="pipeline-node-status">
              <span class="dot"></span>
              ${m.status === 'ready' ? t('badge.available') : t('badge.coming_soon')}
            </div>
          </div>
          ${connector}
        </div>`;
    }).join('');

    const projName = state.projectOpen ? state.projectName : t('project.none_open');
    const projStatus = state.projectOpen
      ? `<span class="badge badge-green" style="margin-left:8px">${t('project.open_badge')}</span>`
      : `<span class="badge badge-muted" style="margin-left:8px">${t('project.closed_badge')}</span>`;

    return `
      <div class="module-view">
        <div class="dashboard-hero animate-slide-up">
          <h1 class="dashboard-title">
            <span class="dashboard-title-accent">${t('dashboard.title_accent')}</span> ${t('dashboard.title_rest')}
          </h1>
          <p class="dashboard-subtitle">
            ${t('dashboard.subtitle')}
          </p>
        </div>

        <div class="card animate-slide-up" style="animation-delay: 40ms; margin-bottom: 16px; padding: 16px 24px;">
          <div class="card-header" style="margin-bottom: 12px">
            <span class="card-title"><i data-lucide="folder-open" style="width:15px;height:15px;vertical-align:-2px;margin-right:6px"></i>${t('project.section_title')}</span>
            ${projStatus}
          </div>
          <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;">
            <span style="font-size:0.9rem;color:var(--text-secondary);flex:1;min-width:120px;" id="dash-proj-name">${projName}</span>
            <button class="btn btn-secondary btn-sm" onclick="projectNew()"><i data-lucide="folder-plus"></i> ${t('project.new')}</button>
            <button class="btn btn-secondary btn-sm" onclick="projectOpen()"><i data-lucide="folder-open"></i> ${t('project.open')}</button>
          </div>
        </div>

        <div class="pipeline-flow-container card animate-slide-up" style="animation-delay: 60ms; padding: 16px 24px;">
          <div class="card-header" style="margin-bottom: 8px">
            <span class="card-title">${t('dashboard.pipeline_section')}</span>
            <span class="badge badge-teal">${t('dashboard.modules_badge', { n: MODULES.length })}</span>
          </div>
          <div class="pipeline-flow stagger">
            ${pipelineNodes}
          </div>
        </div>

        <div class="stats-row stagger">
          <div class="stat-card animate-slide-up">
            <div class="stat-label">${t('dashboard.stat_modules_ready')}</div>
            <div class="stat-value">4<span class="stat-unit">/ 7</span></div>
          </div>
          <div class="stat-card animate-slide-up">
            <div class="stat-label">${t('dashboard.stat_rust_tools')}</div>
            <div class="stat-value">4</div>
          </div>
          <div class="stat-card animate-slide-up">
            <div class="stat-label">${t('dashboard.stat_active_jobs')}</div>
            <div class="stat-value">0</div>
          </div>
          <div class="stat-card animate-slide-up">
            <div class="stat-label">${t('dashboard.stat_speed_gain')}</div>
            <div class="stat-value">28<span class="stat-unit">x</span></div>
          </div>
        </div>

        <div class="dashboard-grid">
          <div class="card animate-slide-up" style="animation-delay: 200ms">
            <div class="card-header">
              <span class="card-title">${t('dashboard.quick_start')}</span>
            </div>
            <div class="quick-actions">
              ${renderQuickAction('qc', 'microscope', 'teal', t('dashboard.quick.qc_title'), t('dashboard.quick.qc_desc'))}
              ${renderQuickAction('trimming', 'scissors', 'blue', t('dashboard.quick.trim_title'), t('dashboard.quick.trim_desc'))}
              ${renderQuickAction('differential', 'flame', 'coral', t('dashboard.quick.deseq_title'), t('dashboard.quick.deseq_desc'))}
              ${renderQuickAction('network', 'share-2', 'green', t('dashboard.quick.wgcna_title'), t('dashboard.quick.wgcna_desc'))}
            </div>
          </div>

          <div class="card animate-slide-up" style="animation-delay: 260ms">
            <div class="card-header">
              <span class="card-title">${t('dashboard.tool_suite')}</span>
            </div>
            <div>
              ${renderToolInfo('fastqc-rs', t('dashboard.tool_desc.fastqc'), 'GPL-3.0')}
              ${renderToolInfo('cutadapt-rs', t('dashboard.tool_desc.cutadapt'), 'MIT')}
              ${renderToolInfo('DESeq2_rs', t('dashboard.tool_desc.deseq2'), 'MIT')}
              ${renderToolInfo('WGCNA_rs', t('dashboard.tool_desc.wgcna'), 'GPL-2.0')}
            </div>
          </div>
        </div>
      </div>`;
  }
```

- [ ] **Step 2: `renderModule()` and `renderComingSoon()` / `renderEmptyState()`**

Replace `renderModule()` (around line 294) — only the header text and the "not found" fallback change:

```js
  function renderModule(moduleId) {
    const mod = MODULES.find(m => m.id === moduleId);
    if (!mod) return renderEmptyState(t('common.module_not_found'));

    const hex = COLOR_MAP[mod.color];
    const nameKey = 'nav.' + mod.id.replace(/-/g, '_');
    const header = `
      <div class="module-header animate-slide-up">
        <div class="module-icon" style="background: ${hex}12; color: ${hex};">
          <i data-lucide="${mod.icon}"></i>
        </div>
        <div>
          <h1 class="module-title">${t(nameKey)}</h1>
          <p class="module-desc">${t('module.powered_by')} <strong style="color: ${hex}">${mod.tool}</strong></p>
          <div class="module-badges">
            <span class="badge badge-${mod.color}">${mod.status === 'ready' ? t('badge.available') : t('badge.coming_soon')}</span>
          </div>
        </div>
      </div>`;

    if (mod.status === 'soon') {
      return `<div class="module-view">${header}${renderComingSoon(mod)}</div>`;
    }

    let content = '';
    switch (moduleId) {
      case 'qc':           content = renderQC(mod); break;
      case 'trimming':     content = renderTrimming(mod); break;
      case 'differential': content = renderDifferential(mod); break;
      case 'network':      content = renderNetwork(mod); break;
      default:             content = renderComingSoon(mod); break;
    }

    return `<div class="module-view">${header}${content}</div>`;
  }
```

Replace `renderComingSoon()` (around line 1021):

```js
  function renderComingSoon(mod) {
    const nameKey = 'nav.' + mod.id.replace(/-/g, '_');
    return `
      <div class="card animate-slide-up" style="animation-delay:100ms">
        <div class="empty-state" style="padding:64px 24px">
          <div class="empty-state-icon"><i data-lucide="${mod.icon}"></i></div>
          <h3 class="empty-state-title">${t(nameKey)}</h3>
          <p class="empty-state-text">${t('module.soon_body', { tool: `<strong>${mod.tool}</strong>` })}</p>
          <div style="margin-top:20px"><span class="badge badge-muted" style="font-size:0.8rem;padding:6px 14px">${t('badge.in_development')}</span></div>
        </div>
      </div>`;
  }
```

(Note: `soon_body` contains `{tool}`; since the value we pass is already `<strong>…</strong>` HTML, and `.empty-state-text` sets `textContent` nowhere — it's injected via `innerHTML` — the HTML is safely rendered. The module's `tool` value comes from the hardcoded `MODULES` array, never user input.)

`renderEmptyState(msg)` is called with already-localised strings — leave it unchanged.

- [ ] **Step 3: `renderQC()` (line ≈331)**

Replace the entire function body:

```js
  function renderQC() {
    return `
      <div class="module-layout">
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:100ms">
            <div class="panel-header">
              <span class="panel-title">${t('qc.input_files')}</span>
              <span class="badge badge-teal">${t('qc.files_count', { n: state.files.qc.length })}</span>
            </div>
            <div class="panel-body">
              <div class="file-drop-zone" data-module="qc" data-accept=".fastq,.fq,.fastq.gz,.fq.gz,.bam,.sam">
                <div class="file-drop-icon"><i data-lucide="upload-cloud"></i></div>
                <div class="file-drop-text">${t('qc.drop_text')}</div>
                <div class="file-drop-hint">${t('qc.drop_hint')}</div>
              </div>
              <div class="file-list" id="qc-file-list"></div>
            </div>
          </div>
          <div class="module-panel animate-slide-up" style="animation-delay:160ms">
            <div class="panel-header"><span class="panel-title">${t('qc.parameters')}</span></div>
            <div class="panel-body">
              <div class="form-row">
                <div class="form-group">
                  <label class="form-label">${t('qc.threads')}</label>
                  <input type="number" class="form-input" id="qc-threads" value="4" min="1" max="32">
                </div>
                <div class="form-group">
                  <label class="form-label">${t('qc.format')}</label>
                  <select class="form-select" id="qc-format">
                    <option>${t('qc.format_auto')}</option><option>FASTQ</option><option>BAM</option><option>SAM</option>
                  </select>
                </div>
              </div>
              <div class="form-group">
                <label class="form-label">${t('qc.output_dir')}</label>
                <input type="text" class="form-input" id="qc-output" placeholder="${t('qc.output_dir_ph')}">
              </div>
              <div class="collapsible">
                <button class="collapsible-trigger" onclick="toggleCollapsible(this)">
                  ${t('common.advanced_options')} <i data-lucide="chevron-down"></i>
                </button>
                <div class="collapsible-content"><div class="collapsible-body">
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="qc-casava"> ${t('qc.casava')}</label></div>
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="qc-nogroup"> ${t('qc.nogroup')}</label></div>
                  <div class="form-group"><label class="form-label">${t('qc.kmer')}</label><input type="number" class="form-input" id="qc-kmer" value="7" min="2" max="10"></div>
                </div></div>
              </div>
            </div>
            <div class="panel-footer">
              <button class="btn btn-secondary btn-sm" onclick="resetForm('qc')"><i data-lucide="rotate-ccw"></i> ${t('common.reset')}</button>
              <button class="btn btn-primary btn-sm" onclick="runModule('qc')"><i data-lucide="play"></i> ${t('qc.run_qc')}</button>
            </div>
            ${renderLogPanel('qc')}
          </div>
        </div>
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:220ms">
            <div class="panel-header"><span class="panel-title">${t('qc.results')}</span></div>
            <div class="panel-body">
              <div class="tabs">
                <div class="tab active" data-tab="qc-chart">${t('qc.tab_quality')}</div>
                <div class="tab" data-tab="qc-summary">${t('qc.tab_summary')}</div>
                <div class="tab" data-tab="qc-log">${t('qc.tab_log')}</div>
              </div>
              <div class="tab-content active" data-tab="qc-chart">
                <div class="chart-container" id="qc-quality-chart" style="height:320px;"></div>
              </div>
              <div class="tab-content" data-tab="qc-summary">
                <div class="results-summary">
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-green)">Pass</div><div class="result-metric-label">${t('qc.metric_overall')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">35.2</div><div class="result-metric-label">${t('qc.metric_mean_quality')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">12.4M</div><div class="result-metric-label">${t('qc.metric_total_reads')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">150</div><div class="result-metric-label">${t('qc.metric_read_length')}</div></div>
                </div>
                <table class="data-table"><thead><tr><th>${t('qc.col_module')}</th><th>${t('qc.col_status')}</th></tr></thead><tbody>
                  <tr><td>Per base sequence quality</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Per sequence quality scores</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Per base sequence content</td><td><span class="badge badge-gold">WARN</span></td></tr>
                  <tr><td>Per sequence GC content</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Per base N content</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Sequence length distribution</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Sequence duplication levels</td><td><span class="badge badge-gold">WARN</span></td></tr>
                  <tr><td>Overrepresented sequences</td><td><span class="badge badge-green">PASS</span></td></tr>
                  <tr><td>Adapter content</td><td><span class="badge badge-green">PASS</span></td></tr>
                </tbody></table>
              </div>
              <div class="tab-content" data-tab="qc-log">
                <div class="log-output"><span class="log-info">[INFO]</span> fastqc-rs v0.12.1
<span class="log-info">[INFO]</span> Processing sample_R1.fastq.gz...
<span class="log-info">[INFO]</span> Threads: 4, Format: auto-detect
<span class="log-success">[DONE]</span> Analysis complete: 12,432,891 reads
<span class="log-info">[INFO]</span> Output written to ./fastqc_output/</div>
              </div>
            </div>
          </div>
        </div>
      </div>`;
  }
```

(Tool names "Per base sequence quality" etc. are fastqc module identifiers — deliberately not translated. The demo log block is out-of-scope mock log output — left English.)

- [ ] **Step 4: `renderTrimming()` (line ≈432)**

Replace the entire function body:

```js
  function renderTrimming() {
    return `
      <div class="module-layout">
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:100ms">
            <div class="panel-header"><span class="panel-title">${t('trimming.input_files')}</span></div>
            <div class="panel-body">
              <div class="file-drop-zone" data-module="trimming" data-accept=".fastq,.fq,.fastq.gz,.fq.gz">
                <div class="file-drop-icon"><i data-lucide="upload-cloud"></i></div>
                <div class="file-drop-text">${t('trimming.drop_text')}</div>
                <div class="file-drop-hint">${t('trimming.drop_hint')}</div>
              </div>
              <div class="file-list" id="trimming-file-list"></div>
            </div>
          </div>
          <div class="module-panel animate-slide-up" style="animation-delay:160ms">
            <div class="panel-header"><span class="panel-title">${t('trimming.adapter_settings')}</span></div>
            <div class="panel-body">
              <div class="form-group">
                <label class="form-label">${t('trimming.adapter_preset')}</label>
                <select class="form-select" id="trim-preset">
                  <option>${t('trimming.preset_illumina')}</option>
                  <option>${t('trimming.preset_nextera')}</option>
                  <option>${t('trimming.preset_smallrna')}</option>
                  <option>${t('trimming.preset_bgi')}</option>
                  <option>${t('trimming.preset_custom')}</option>
                </select>
              </div>
              <div class="form-group">
                <label class="form-label">${t('trimming.adapter_3')}</label>
                <input type="text" class="form-input" id="trim-adapter" value="AGATCGGAAGAGC" placeholder="AGATCGGAAGAGC">
                <span class="form-hint">${t('trimming.adapter_3_hint')}</span>
              </div>
              <div class="form-row">
                <div class="form-group"><label class="form-label">${t('trimming.quality_cutoff')}</label><input type="number" class="form-input" id="trim-quality" value="20" min="0" max="42"></div>
                <div class="form-group"><label class="form-label">${t('trimming.min_length')}</label><input type="number" class="form-input" id="trim-minlen" value="20" min="1"></div>
              </div>
              <div class="form-row">
                <div class="form-group"><label class="form-label">${t('trimming.max_n')}</label><input type="number" class="form-input" id="trim-maxn" value="-1"><span class="form-hint">${t('trimming.max_n_hint')}</span></div>
                <div class="form-group"><label class="form-label">${t('trimming.threads')}</label><input type="number" class="form-input" id="trim-threads" value="4" min="1" max="16"></div>
              </div>
              <div class="collapsible">
                <button class="collapsible-trigger" onclick="toggleCollapsible(this)">${t('trimming.paired_options')} <i data-lucide="chevron-down"></i></button>
                <div class="collapsible-content"><div class="collapsible-body">
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="trim-paired"> ${t('trimming.paired_mode')}</label></div>
                  <div class="form-group"><label class="form-label">${t('trimming.adapter_r2')}</label><input type="text" class="form-input" id="trim-adapter2" placeholder="AGATCGGAAGAGC"></div>
                </div></div>
              </div>
              <div class="collapsible">
                <button class="collapsible-trigger" onclick="toggleCollapsible(this)">${t('trimming.trim_galore')} <i data-lucide="chevron-down"></i></button>
                <div class="collapsible-content"><div class="collapsible-body">
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="trim-galore"> ${t('trimming.enable_galore')}</label></div>
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="trim-fastqc"> ${t('trimming.post_fastqc')}</label></div>
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="trim-rrbs"> ${t('trimming.rrbs')}</label></div>
                </div></div>
              </div>
            </div>
            <div class="panel-footer">
              <button class="btn btn-secondary btn-sm" onclick="resetForm('trimming')"><i data-lucide="rotate-ccw"></i> ${t('common.reset')}</button>
              <button class="btn btn-primary btn-sm" onclick="runModule('trimming')" style="background:var(--mod-blue);border-color:var(--mod-blue)"><i data-lucide="play"></i> ${t('trimming.run_trim')}</button>
            </div>
            ${renderLogPanel('trimming')}
          </div>
        </div>
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:220ms">
            <div class="panel-header"><span class="panel-title">${t('trimming.results')}</span></div>
            <div class="panel-body">
              <div class="tabs">
                <div class="tab active" data-tab="trim-stats">${t('trimming.tab_stats')}</div>
                <div class="tab" data-tab="trim-chart">${t('trimming.tab_chart')}</div>
                <div class="tab" data-tab="trim-log">${t('qc.tab_log')}</div>
              </div>
              <div class="tab-content active" data-tab="trim-stats">
                <div class="results-summary">
                  <div class="result-metric"><div class="result-metric-value">10.2M</div><div class="result-metric-label">${t('trimming.metric_reads_processed')}</div></div>
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-green)">98.7%</div><div class="result-metric-label">${t('trimming.metric_reads_passing')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">4.3%</div><div class="result-metric-label">${t('trimming.metric_adapter_found')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">142</div><div class="result-metric-label">${t('trimming.metric_mean_length')}</div></div>
                </div>
                <table class="data-table"><thead><tr><th>${t('trimming.col_metric')}</th><th>${t('trimming.col_value')}</th></tr></thead><tbody>
                  <tr><td>Total reads processed</td><td>10,243,891</td></tr>
                  <tr><td>Reads with adapters</td><td>438,215 (4.3%)</td></tr>
                  <tr><td>Reads too short</td><td>132,045 (1.3%)</td></tr>
                  <tr><td>Reads passing filters</td><td>10,111,846 (98.7%)</td></tr>
                  <tr><td>Base pairs processed</td><td>1,536,583,650</td></tr>
                  <tr><td>Quality-trimmed</td><td>12,456,789 bp (0.8%)</td></tr>
                  <tr><td>Total written</td><td>1,435,822,132 bp (93.4%)</td></tr>
                </tbody></table>
              </div>
              <div class="tab-content" data-tab="trim-chart">
                <div class="chart-container" id="trim-length-chart" style="height:320px;"></div>
              </div>
              <div class="tab-content" data-tab="trim-log">
                <div class="log-output"><span class="log-info">[INFO]</span> cutadapt-rs v0.1.0
<span class="log-info">[INFO]</span> Adapter: AGATCGGAAGAGC (3' regular)
<span class="log-info">[INFO]</span> Quality cutoff: 20, Min length: 20
<span class="log-info">[INFO]</span> Processing with 4 threads...
<span class="log-success">[DONE]</span> 10,243,891 reads processed in 48.2s
<span class="log-info">[INFO]</span> Output: trimmed_R1.fastq.gz</div>
              </div>
            </div>
          </div>
        </div>
      </div>`;
  }
```

- [ ] **Step 5: `renderStarIndex()` and `renderStarAlign()` (line ≈541, ≈590)**

Replace `renderStarIndex()`:

```js
  function renderStarIndex() {
    return `
    <h2>${t('star_index.title')}</h2>
    <p>${t('star_index.desc')}</p>
    <form id="form-star-index">
      <label>${t('star_index.genome_fasta')}
        <input type="text" name="genome_fasta" data-pick="file" placeholder="/path/to/genome.fa" required />
        <button type="button" data-pick-for="genome_fasta">${t('common.browse')}</button>
      </label>
      <label>${t('star_index.gtf')}
        <input type="text" name="gtf_file" data-pick="file" placeholder="/path/to/annotation.gtf" required />
        <button type="button" data-pick-for="gtf_file">${t('common.browse')}</button>
      </label>
      <label>${t('star_index.threads')} <input type="number" name="threads" value="4" min="1" /></label>
      <label>${t('star_index.sjdb')} <input type="number" name="sjdb_overhang" value="100" min="1" /></label>
      <label>${t('star_index.sa_nbases')} <input type="number" name="genome_sa_index_nbases" value="14" min="1" max="18" /></label>
      <details><summary>${t('star_index.advanced')}</summary>
        <label>${t('star_index.extra_args')}
          <textarea name="extra_args" placeholder="--limitGenomeGenerateRAM&#10;31000000000"></textarea>
        </label>
      </details>
      <button type="submit">${t('star_index.submit')}</button>
    </form>
    <div id="star-index-runs"></div>
    ${renderLogPanel('star_index')}
  `;
  }
```

Replace `renderStarAlign()`:

```js
  function renderStarAlign() {
    return `
    <h2>${t('star_align.title')}</h2>
    <p>${t('star_align.desc')}</p>
    <form id="form-star-align">
      <label>${t('star_align.genome_dir')}
        <input type="text" name="genome_dir" required placeholder="/path/to/star_index" />
        <button type="button" data-pick-for="genome_dir" data-pick-mode="dir">${t('common.browse')}</button>
      </label>
      <label>${t('star_align.reads_1')}
        <input type="text" name="reads_1" required placeholder="/path/to/S1_R1.fq.gz /path/to/S2_R1.fq.gz" />
        <button type="button" data-pick-for="reads_1" data-pick-mode="multi">${t('common.browse')}</button>
      </label>
      <label>${t('star_align.reads_2')}
        <input type="text" name="reads_2" placeholder="/path/to/S1_R2.fq.gz /path/to/S2_R2.fq.gz" />
        <button type="button" data-pick-for="reads_2" data-pick-mode="multi">${t('common.browse')}</button>
      </label>
      <label>${t('star_align.sample_names')}
        <textarea name="sample_names" placeholder="S1&#10;S2"></textarea>
      </label>
      <label>${t('star_align.threads')} <input type="number" name="threads" value="4" min="1" /></label>
      <fieldset>
        <legend>${t('star_align.strand')}</legend>
        <label><input type="radio" name="strand" value="unstranded" checked /> ${t('star_align.strand_unstranded')}</label>
        <label><input type="radio" name="strand" value="forward" /> ${t('star_align.strand_forward')}</label>
        <label><input type="radio" name="strand" value="reverse" /> ${t('star_align.strand_reverse')}</label>
      </fieldset>
      <details><summary>${t('star_align.advanced')}</summary>
        <label>${t('star_align.extra_args')}
          <textarea name="extra_args" placeholder="--outFilterMultimapNmax&#10;10"></textarea>
        </label>
      </details>
      <button type="submit">${t('star_align.submit')}</button>
    </form>
    <div id="star-align-runs"></div>
    ${renderLogPanel('star_align')}
  `;
  }
```

Replace `renderStarAlignResult()` header text (around line 683–690) — only the three headings / button / fallback change. Find:
```js
    return `
    <h3>Mapping rate</h3>
    <div id="${chartId}" style="width: 100%; height: 320px;"></div>
    <h3>Counts matrix preview (first 50 × 10)</h3>
    <div id="${previewId}">Loading…</div>
    ${matrixPath ? `<button id="${btnId}" data-matrix="${matrixPath}">Use this matrix in DESeq2</button>` : ''}
    ${previewHtml}
  `;
```
Replace with:
```js
    return `
    <h3>${t('star_align.mapping_rate')}</h3>
    <div id="${chartId}" style="width: 100%; height: 320px;"></div>
    <h3>${t('star_align.matrix_preview')}</h3>
    <div id="${previewId}">${t('common.loading')}</div>
    ${matrixPath ? `<button id="${btnId}" data-matrix="${matrixPath}">${t('star_align.use_in_deseq')}</button>` : ''}
    ${previewHtml}
  `;
```

Also find (same function, around line 681):
```js
    } else {
      previewHtml = '<p><em>No counts matrix produced</em></p>';
    }
```
Replace with:
```js
    } else {
      previewHtml = `<p><em>${t('star_align.no_matrix')}</em></p>`;
    }
```

- [ ] **Step 6: `renderDifferential()` (line ≈713)**

Replace the entire function body:

```js
  function renderDifferential() {
    const prefill = (state.prefill && state.prefill.differential) || {};
    state.prefill = {};
    return `
      <div class="module-layout">
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:100ms">
            <div class="panel-header"><span class="panel-title">${t('differential.input_data')}</span></div>
            <div class="panel-body">
              <div class="form-group">
                <label class="form-label">${t('differential.counts_matrix')}</label>
                ${prefill.counts_matrix
                  ? `<input type="text" class="form-input" id="deseq-counts-matrix" value="${prefill.counts_matrix}" placeholder="${t('differential.counts_matrix_ph')}">`
                  : `<div class="file-drop-zone" data-module="differential" data-accept=".tsv,.csv,.txt" style="padding:20px">
                  <div class="file-drop-icon" style="margin-bottom:8px"><i data-lucide="table"></i></div>
                  <div class="file-drop-text" style="font-size:0.85rem">${t('differential.drop_counts')}</div>
                  <div class="file-drop-hint">${t('differential.drop_counts_hint')}</div>
                </div>`}
              </div>
              <div class="form-group">
                <label class="form-label">${t('differential.sample_info')}</label>
                <div class="file-drop-zone" data-module="differential-coldata" data-accept=".tsv,.csv,.txt" style="padding:20px">
                  <div class="file-drop-icon" style="margin-bottom:8px"><i data-lucide="file-text"></i></div>
                  <div class="file-drop-text" style="font-size:0.85rem">${t('differential.drop_coldata')}</div>
                  <div class="file-drop-hint">${t('differential.drop_coldata_hint')}</div>
                </div>
              </div>
            </div>
          </div>
          <div class="module-panel animate-slide-up" style="animation-delay:160ms">
            <div class="panel-header"><span class="panel-title">${t('differential.parameters')}</span></div>
            <div class="panel-body">
              <div class="form-group">
                <label class="form-label">${t('differential.design_var')}</label>
                <input type="text" class="form-input" id="deseq-design" value="condition" placeholder="${t('differential.design_var_ph')}">
                <span class="form-hint">${t('differential.design_var_hint')}</span>
              </div>
              <div class="form-group">
                <label class="form-label">${t('differential.ref_level')}</label>
                <input type="text" class="form-input" id="deseq-ref" value="control" placeholder="${t('differential.ref_level_ph')}">
                <span class="form-hint">${t('differential.ref_level_hint')}</span>
              </div>
              <div class="form-row">
                <div class="form-group"><label class="form-label">${t('differential.padj')}</label><input type="number" class="form-input" id="deseq-padj" value="0.01" step="0.01" min="0" max="1"></div>
                <div class="form-group"><label class="form-label">${t('differential.lfc')}</label><input type="number" class="form-input" id="deseq-lfc" value="1.0" step="0.1" min="0"></div>
              </div>
              <div class="form-group">
                <label class="form-label">${t('differential.output_file')}</label>
                <input type="text" class="form-input" id="deseq-output" value="deseq2_results.tsv" placeholder="results.tsv">
              </div>
            </div>
            <div class="panel-footer">
              <button class="btn btn-secondary btn-sm" onclick="resetForm('differential')"><i data-lucide="rotate-ccw"></i> ${t('common.reset')}</button>
              <button class="btn btn-primary btn-sm" onclick="runModule('differential')" style="background:var(--mod-coral);border-color:var(--mod-coral)"><i data-lucide="play"></i> ${t('differential.run_deseq')}</button>
            </div>
            ${renderLogPanel('differential')}
          </div>
        </div>
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:220ms">
            <div class="panel-header"><span class="panel-title">${t('differential.results')}</span></div>
            <div class="panel-body">
              <div class="tabs">
                <div class="tab active" data-tab="deseq-volcano">${t('differential.tab_volcano')}</div>
                <div class="tab" data-tab="deseq-ma">${t('differential.tab_ma')}</div>
                <div class="tab" data-tab="deseq-table">${t('differential.tab_table')}</div>
                <div class="tab" data-tab="deseq-custom">${t('differential.tab_custom')}</div>
                <div class="tab" data-tab="deseq-log">${t('qc.tab_log')}</div>
              </div>
              <div class="tab-content active" data-tab="deseq-volcano">
                <div class="results-summary">
                  <div class="result-metric"><div class="result-metric-value">64,102</div><div class="result-metric-label">${t('differential.metric_total')}</div></div>
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-coral)">347</div><div class="result-metric-label">${t('differential.metric_up')}</div></div>
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-blue)">325</div><div class="result-metric-label">${t('differential.metric_down')}</div></div>
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-teal)">672</div><div class="result-metric-label">${t('differential.metric_sig')}</div></div>
                </div>
                <div class="chart-container" id="deseq-volcano-chart" style="height:380px;"></div>
              </div>
              <div class="tab-content" data-tab="deseq-ma">
                <div class="chart-container" id="deseq-ma-chart" style="height:380px;"></div>
              </div>
              <div class="tab-content" data-tab="deseq-table">
                <div style="display:flex;justify-content:flex-end;margin-bottom:8px;">
                  <button class="btn btn-ghost btn-sm" onclick="exportTableAsTSV('deseq-results-table', 'deseq2_results.tsv')">${t('common.export_tsv')}</button>
                </div>
                <div style="max-height:400px;overflow-y:auto;">
                  <table class="data-table" id="deseq-results-table"><thead><tr><th>${t('differential.col_gene')}</th><th>${t('differential.col_lfc')}</th><th>${t('differential.col_pvalue')}</th><th>${t('differential.col_padj')}</th></tr></thead><tbody></tbody></table>
                </div>
              </div>
              <div class="tab-content" data-tab="deseq-custom">
                ${renderCustomPlotPanel('differential')}
              </div>
              <div class="tab-content" data-tab="deseq-log">
                <div class="log-output"><span class="log-info">[INFO]</span> DESeq2_rs v0.1.0
<span class="log-info">[INFO]</span> Counts: 64,102 genes x 8 samples
<span class="log-info">[INFO]</span> Design: ~condition, Reference: control
<span class="log-info">[INFO]</span> Estimating size factors...
<span class="log-info">[INFO]</span> Estimating dispersions...
<span class="log-info">[INFO]</span> Fitting NB GLM (IRLS)...
<span class="log-info">[INFO]</span> Wald test + BH adjustment...
<span class="log-success">[DONE]</span> 672 significant genes (padj &lt; 0.01, |log2FC| &gt; 1)
<span class="log-info">[INFO]</span> Output: deseq2_results.tsv</div>
              </div>
            </div>
          </div>
        </div>
      </div>`;
  }
```

- [ ] **Step 7: `renderNetwork()` (line ≈824)**

Replace the entire function body:

```js
  function renderNetwork() {
    return `
      <div class="module-layout">
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:100ms">
            <div class="panel-header"><span class="panel-title">${t('network.input_data')}</span></div>
            <div class="panel-body">
              <div class="form-group">
                <label class="form-label">${t('network.expr_matrix')}</label>
                <div class="file-drop-zone" data-module="network" data-accept=".csv,.tsv" style="padding:20px">
                  <div class="file-drop-icon" style="margin-bottom:8px"><i data-lucide="grid-3x3"></i></div>
                  <div class="file-drop-text" style="font-size:0.85rem">${t('network.drop_expr')}</div>
                  <div class="file-drop-hint">${t('network.drop_expr_hint')}</div>
                </div>
              </div>
              <div class="form-group">
                <label class="form-label">${t('network.trait_data')}</label>
                <div class="file-drop-zone" data-module="network-trait" data-accept=".csv,.tsv" style="padding:20px">
                  <div class="file-drop-icon" style="margin-bottom:8px"><i data-lucide="file-text"></i></div>
                  <div class="file-drop-text" style="font-size:0.85rem">${t('network.drop_trait')}</div>
                  <div class="file-drop-hint">${t('network.drop_trait_hint')}</div>
                </div>
              </div>
            </div>
          </div>
          <div class="module-panel animate-slide-up" style="animation-delay:160ms">
            <div class="panel-header"><span class="panel-title">${t('network.parameters')}</span></div>
            <div class="panel-body">
              <div class="form-group"><label class="form-label">${t('network.corr_method')}</label>
                <select class="form-select" id="wgcna-corr"><option>${t('network.corr_pearson')}</option><option>${t('network.corr_biweight')}</option></select></div>
              <div class="form-group"><label class="form-label">${t('network.net_type')}</label>
                <select class="form-select" id="wgcna-nettype"><option>${t('network.net_signed')}</option><option>${t('network.net_unsigned')}</option><option>${t('network.net_signed_hybrid')}</option></select></div>
              <div class="form-row">
                <div class="form-group"><label class="form-label">${t('network.soft_thresh')}</label><input type="number" class="form-input" id="wgcna-thresh" value="6" min="1" max="30"><span class="form-hint">${t('network.soft_thresh_hint')}</span></div>
                <div class="form-group"><label class="form-label">${t('network.min_module')}</label><input type="number" class="form-input" id="wgcna-minmod" value="30" min="10"></div>
              </div>
              <div class="form-row">
                <div class="form-group"><label class="form-label">${t('network.merge_cut')}</label><input type="number" class="form-input" id="wgcna-mergecut" value="0.25" step="0.05" min="0" max="1"></div>
                <div class="form-group"><label class="form-label">${t('network.tom_type')}</label>
                  <select class="form-select" id="wgcna-tom"><option>${t('network.net_signed')}</option><option>${t('network.net_unsigned')}</option></select></div>
              </div>
              <div class="collapsible">
                <button class="collapsible-trigger" onclick="toggleCollapsible(this)">${t('common.advanced_options')} <i data-lucide="chevron-down"></i></button>
                <div class="collapsible-content"><div class="collapsible-body">
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="wgcna-pam"> ${t('network.pam')}</label></div>
                  <div class="form-group"><label class="form-label">${t('network.deep_split')}</label>
                    <select class="form-select" id="wgcna-deepsplit"><option value="0">0</option><option value="1">1</option><option value="2" selected>${t('network.deep_default')}</option><option value="3">3</option><option value="4">4</option></select></div>
                  <div class="form-group"><label class="form-checkbox"><input type="checkbox" id="wgcna-cytoscape"> ${t('network.cytoscape')}</label></div>
                </div></div>
              </div>
            </div>
            <div class="panel-footer">
              <button class="btn btn-secondary btn-sm"><i data-lucide="zap"></i> ${t('network.pick_threshold')}</button>
              <button class="btn btn-primary btn-sm" onclick="runModule('network')" style="background:var(--mod-green);border-color:var(--mod-green)"><i data-lucide="play"></i> ${t('network.run_wgcna')}</button>
            </div>
            ${renderLogPanel('network')}
          </div>
        </div>
        <div>
          <div class="module-panel animate-slide-up" style="animation-delay:220ms">
            <div class="panel-header"><span class="panel-title">${t('network.results')}</span></div>
            <div class="panel-body">
              <div class="tabs">
                <div class="tab active" data-tab="wgcna-modules">${t('network.tab_modules')}</div>
                <div class="tab" data-tab="wgcna-trait">${t('network.tab_trait')}</div>
                <div class="tab" data-tab="wgcna-custom">${t('network.tab_custom')}</div>
                <div class="tab" data-tab="wgcna-log">${t('qc.tab_log')}</div>
              </div>
              <div class="tab-content active" data-tab="wgcna-modules">
                <div class="results-summary">
                  <div class="result-metric"><div class="result-metric-value">5,000</div><div class="result-metric-label">${t('network.metric_genes')}</div></div>
                  <div class="result-metric"><div class="result-metric-value" style="color:var(--mod-green)">12</div><div class="result-metric-label">${t('network.metric_modules')}</div></div>
                  <div class="result-metric"><div class="result-metric-value">R²=0.87</div><div class="result-metric-label">${t('network.metric_fit')}</div></div>
                </div>
                <div class="chart-container" id="wgcna-module-chart" style="height:320px;"></div>
              </div>
              <div class="tab-content" data-tab="wgcna-trait">
                <div class="chart-container" id="wgcna-trait-chart" style="height:380px;"></div>
              </div>
              <div class="tab-content" data-tab="wgcna-custom">
                ${renderCustomPlotPanel('network')}
              </div>
              <div class="tab-content" data-tab="wgcna-log">
                <div class="log-output"><span class="log-info">[INFO]</span> WGCNA_rs v0.1.0
<span class="log-info">[INFO]</span> Matrix: 50 samples x 5,000 genes
<span class="log-info">[INFO]</span> Correlation: Pearson, Network: Signed
<span class="log-info">[INFO]</span> Soft threshold = 6 (R² = 0.87)
<span class="log-info">[INFO]</span> Computing TOM...
<span class="log-info">[INFO]</span> Hierarchical clustering (NN-chain)...
<span class="log-info">[INFO]</span> Dynamic tree cutting...
<span class="log-info">[INFO]</span> Merging modules (cutHeight=0.25)...
<span class="log-success">[DONE]</span> 12 modules identified
<span class="log-info">[INFO]</span> Output: module_assignments.tsv</div>
              </div>
            </div>
          </div>
        </div>
      </div>`;
  }
```

- [ ] **Step 8: `renderLogPanel()` (line ≈1572)**

Replace:
```js
  function renderLogPanel(panelKey) {
    const existing = state.logsByRun[panelKey] || [];
    const text = existing.map(e => (e.stream === 'stderr' ? '' : '[out] ') + e.line).join('\n');
    return `<details class="log-panel" data-log-panel="${panelKey}">
    <summary>Log</summary>
    <pre>${escapeHtml(text)}</pre>
  </details>`;
  }
```

With:
```js
  function renderLogPanel(panelKey) {
    const existing = state.logsByRun[panelKey] || [];
    const text = existing.map(e => (e.stream === 'stderr' ? '' : '[out] ') + e.line).join('\n');
    return `<details class="log-panel" data-log-panel="${panelKey}">
    <summary>${t('common.log_panel')}</summary>
    <pre>${escapeHtml(text)}</pre>
  </details>`;
  }
```

- [ ] **Step 9: `runModule()` status text + `projectNew()` prompt**

In `runModule` (around line 1527), replace:
```js
    st.textContent = `Running ${mod?.name || id}...`;
    js.textContent = '1 active job';
```
With:
```js
    const nameKey = 'nav.' + (mod ? mod.id.replace(/-/g, '_') : id);
    st.textContent = `${t('status.running_prefix')} ${mod ? t(nameKey) : id}…`;
    js.textContent = t('status.one_job');
```

Also in the same function, replace:
```js
    st.textContent = 'Ready'; js.textContent = 'No active jobs';
    if (badge) { badge.className = 'nav-badge done'; badge.textContent = 'Done'; }
```
With:
```js
    st.textContent = t('status.ready'); js.textContent = t('status.no_jobs');
    if (badge) { badge.className = 'nav-badge done'; badge.textContent = t('badge.done'); }
```

And the `running` badge assignment just above (line ≈1534):
```js
    if (badge) { badge.className = 'nav-badge running'; badge.textContent = 'Running'; }
```
Replace with:
```js
    if (badge) { badge.className = 'nav-badge running'; badge.textContent = t('badge.running'); }
```

In `window.projectNew` (around line 1258), replace:
```js
    const name = prompt('Enter project name:');
```
With:
```js
    const name = prompt(t('project.prompt_name'));
```

Also in `init()` `run-progress` / `run-completed` / `run-failed` listeners (around line 1596–1613) — these read payload messages from the Rust backend (out of scope). The fallbacks should still use i18n:

Replace:
```js
      if (st) st.textContent = event.payload?.message || 'Running...';
```
With:
```js
      if (st) st.textContent = event.payload?.message || (t('status.running_prefix') + '…');
```

Replace:
```js
      if (st) st.textContent = 'Ready';
      if (js) js.textContent = 'No active jobs';
```
With:
```js
      if (st) st.textContent = t('status.ready');
      if (js) js.textContent = t('status.no_jobs');
```

Leave `event.payload.message` usage as-is when it's present (that's backend-originated text — out of scope).

- [ ] **Step 10: Manual check — every view, both languages**

Reload the browser. For each view in the sidebar (Dashboard, QC, Trimming, Read Alignment, Quantification, Differential, Network, Enrichment, STAR Index, STAR Alignment, Settings), verify:
- English mode: every in-scope UI chrome string is English (no leaked keys like `nav.qc`).
- Chinese mode: same strings are Chinese; out-of-scope strings (chart labels, mock log output, table row identifiers like "Per base sequence quality", table data like "10,243,891") remain English.
- Clicking the toggle re-renders without reload.
- Console has no `[i18n] keys only in …` warnings.

- [ ] **Step 11: Commit**

```bash
git add frontend/js/app.js
git commit -m "feat(frontend): translate render functions UI chrome via I18N.t()"
```

---

## Task 6 — Add Settings "Language" section (radio + sync)

**Files:**
- Modify: `frontend/js/app.js`

- [ ] **Step 1: Restructure `renderSettings()` with nav + Language section**

Replace the entire `renderSettings()` function (around line 1039) with:

```js
  async function renderSettings() {
    let statuses = [];
    try {
      statuses = await window.__TAURI__.core.invoke('get_binary_paths');
    } catch (e) {
      return `<div class="error">Failed to load settings: ${e}</div>`;
    }
    const rows = statuses.map(s => {
      const available = s.configured_path || s.bundled_path || s.detected_on_path;
      return `
      <tr>
        <td>${s.display_name}</td>
        <td class="path">${s.configured_path ? escapeHtml(s.configured_path) : `<em>${t('settings.not_set')}</em>`}</td>
        <td class="path">${s.bundled_path ? escapeHtml(s.bundled_path) : `<em>${t('settings.not_bundled')}</em>`}</td>
        <td class="path">${s.detected_on_path ? escapeHtml(s.detected_on_path) : `<em>${t('settings.not_on_path')}</em>`}</td>
        <td>${available ? `<span class="ok">${t('settings.ok')}</span>` : `<span class="warn">${t('settings.missing')}</span>`}</td>
        <td>
          <button data-act="browse" data-id="${s.id}">${t('common.browse')}</button>
          ${s.configured_path ? `<button data-act="clear" data-id="${s.id}">${t('settings.clear')}</button>` : ''}
        </td>
      </tr>
    `;
    }).join('');
    const cur = window.I18N ? window.I18N.getLang() : 'en';
    return `
      <h2>${t('settings.binary_title')}</h2>
      <p>${t('settings.binary_intro_html')}</p>
      <table class="settings-table">
        <thead><tr>
          <th>${t('settings.col_tool')}</th>
          <th>${t('settings.col_configured')}</th>
          <th>${t('settings.col_bundled')}</th>
          <th>${t('settings.col_path')}</th>
          <th>${t('settings.col_status')}</th>
          <th>${t('settings.col_actions')}</th>
        </tr></thead>
        <tbody>${rows}</tbody>
      </table>

      <h2 style="margin-top:32px">${t('settings.language_section_full')}</h2>
      <div class="settings-language">
        <label><input type="radio" name="lang-choice" value="en" ${cur === 'en' ? 'checked' : ''}> ${t('settings.language_en')}</label>
        <label style="margin-left:16px"><input type="radio" name="lang-choice" value="zh" ${cur === 'zh' ? 'checked' : ''}> ${t('settings.language_zh')}</label>
      </div>
    `;
  }
```

- [ ] **Step 2: Wire the radio to `I18N.setLang`**

In `setupEvents()` (around line 1361), inside the body — right after the `.lang-btn` handler block from Task 4 — add:

```js
    // Settings: language radio
    document.addEventListener('change', (e) => {
      const r = e.target.closest('input[name="lang-choice"]');
      if (r && window.I18N) window.I18N.setLang(r.value);
    });
```

(Placement doesn't need to be exact — anywhere inside `setupEvents()` works. Adjacent to the lang-btn handler is cleanest.)

- [ ] **Step 3: Manual check**

Reload the browser, navigate to Settings:
- The "Language / 语言" section appears at the bottom, with two radios matching the header toggle's current state.
- Switching via the radio updates the header toggle immediately (the `langchange` event triggers both handlers + the Settings re-render via `navigate(state.currentView)`).
- Switching via the header toggle updates the radio after navigate re-renders the Settings view.
- Binary Paths table rows show localized `(not set)` / `OK` / `Missing` etc.

- [ ] **Step 4: Commit**

```bash
git add frontend/js/app.js
git commit -m "feat(frontend): add Settings language section synced with header toggle"
```

---

## Task 7 — End-to-end verification

**Files:** none (verification only)

- [ ] **Step 1: Full manual checklist**

Run the dev server:
```bash
cd frontend && python3 -m http.server 8090
```

Execute every item from the spec's §Testing strategy, one by one:

1. Clear `localStorage` (`localStorage.removeItem('rustbrain.lang')`). Set browser language to English (or start private window). Reload — UI renders in English. `<html>` has `lang="en"`.
2. Clear `localStorage` again. Set browser language to `zh-CN`. Reload — UI renders in Chinese. `<html>` has `lang="zh-CN"`.
3. With either starting language, click the header toggle the other way — UI switches live, no reload. Sidebar, breadcrumb, header "No Project", status bar, currently visible module view all update.
4. Navigate to Settings. The Language radio matches the header toggle. Change it; header toggle updates.
5. Reload. Language choice persists.
6. Navigate through every view: Dashboard, QC, Trimming, Read Alignment (soon), Quantification (soon), Differential, Network, Enrichment (soon), STAR Index, STAR Alignment, Settings. In Chinese:
   - UI chrome is Chinese (labels, buttons, section titles, empty states, badges, tabs).
   - ECharts toolbox "Save PNG" / "Zoom" / legend / axis stays English.
   - Demo log panes stay English.
   - Table rows like "Per base sequence quality", "Total reads processed", numbers stay English.
   - **CJK rendering check**: Chinese glyphs display with a clean CJK fallback font (not a boxes-for-missing-glyphs mess). If on Linux / Windows and the system CJK font looks poor, add `'PingFang SC', 'Microsoft YaHei', 'Noto Sans CJK SC'` to the `.sidebar-nav`, `.breadcrumb`, `.status-bar`, `.panel-title`, `.module-title`, `.btn`, `.form-label`, and `body` `font-family` declarations in `frontend/css/style.css` (append — do not remove Zilla Slab / Karla). Re-verify step 6 after the change.
   - **Badge width check**: sidebar badges "就绪" / "即将推出" should not overflow the nav item. If "即将推出" wraps, shorten to "即将" in the dictionary.
7. Start a module run (mock backend). Progress text / run log lines / validate errors stay English. Status bar "Running …" prefix is localized; backend `event.payload.message` content is English.
8. Browser console: no warnings like `[i18n] keys only in en:` or `[i18n] keys only in zh:`. No `TypeError: Cannot read properties of undefined`.

If any verification step fails, fix the problem inline (not as a new task) — it's a regression from one of Tasks 1–6.

- [ ] **Step 2: Final commit (if any fixes were made)**

```bash
git add -A
git commit -m "fix(frontend): address i18n verification findings"
```

(Skip if step 1 found nothing.)

---

## Notes for implementers

- Every translated string comes from the dictionary inside `i18n.js`. If you encounter a literal UI string in `app.js` that's in-scope (see spec §Translation coverage) but has no matching dictionary entry, **add the key to both `en` and `zh`** — never leave orphan keys.
- If adding a key, run the dev-only check (`console.warn` at the bottom of `i18n.js`) by reloading once; asymmetric keys are reported.
- Do NOT translate: ECharts chart config (`renderMappingRateChart`, `renderQCCharts`, `renderTrimmingCharts`, `renderDESeq2Charts`, `renderWGCNACharts`, `renderCustomPlotPanel`'s axis/legend options), `escapeHtml`, `exportTableAsTSV`, mock demo log blocks, `console.log` outputs, Rust-side `run-log` / `run-progress` / `run-failed` payload strings.
- If something breaks, `localStorage.removeItem('rustbrain.lang')` + reload resets to browser-default.
