//! Generic-path file tools (file_read, file_list, file_write, file_patch).
//! Real implementations land in Task 14+.

use crate::tools::ToolRegistry;

pub fn register(_registry: &mut ToolRegistry) {
    // populated in later tasks
}
